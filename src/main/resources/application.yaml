spring:
  profiles:
    include:
      oauth
  datasource:
    url: jdbc:h2:tcp://localhost/~/oauth
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto:
        update
    show-sql:
      true
